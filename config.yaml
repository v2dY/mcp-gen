# OpenAPI to MCP Generator Configuration
# Sample configuration file

# Basic server information
server_name: "Generated MCP Server"
server_version: "1.0.0"
description: "MCP server generated from OpenAPI specification"
author: "Generated"

# Python settings
python_version: "3.8"

# Generation options
include_examples: true
generate_tests: true

# Output format (currently only 'python' is supported)
output_format: "python"

# Optional: Override base URL for API calls
# base_url: "https://api.example.com"
